# Generate with: openssl rand -base64 32
AUTH_SECRET=

# From Google Cloud Console → APIs & Services → Credentials
AUTH_GOOGLE_ID=your-client-id.apps.googleusercontent.com
AUTH_GOOGLE_SECRET=your-client-secret

# Comma-separated list of up to 5 emails allowed to sign in
# Example: AUTHORIZED_EMAILS=alice@gmail.com,bob@gmail.com
AUTHORIZED_EMAILS=you@gmail.com

# From Vercel Postgres dashboard → Storage → your db → .env.local tab
# Use the non-pooling (direct) connection — required for Prisma migrations and runtime
fast_track_POSTGRES_URL=postgresql://...
fast_track_DATABASE_URL_UNPOOLED=postgresql://...
